[build]
  command = "cd src/vanilla && npm install && npm run build"
  publish = "src/vanilla/dist"

[build.environment]
  NETLIFY_USE_YARN = "false"
  NODE_VERSION = "20"

[template.environment]
  LIFF_ID = "Your LIFF ID which is aquired from LINE Dev Center"
